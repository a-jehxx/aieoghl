# HSM — 집 물건 보관 관리 웹앱

## 1. 앱 소개
- 문제: 집 안 물건을 어디에 뒀는지 몰라서 찾는 데 시간을 낭비한다. (예: 볼펜심)
- 해결: 도면과 사진으로 물건 위치를 기록해 두고, 물건 이름을 검색하면 방 → 가구 → 보관함 → 물건 순으로 깜빡여서 위치를 안내한다. 가족이 코드로 같은 집 데이터를 공유한다.
- 사용자: 집을 정리하며 위치를 기록하는 사람, 물건을 찾는 사람 (같은 사람일 수 있음)
- 형태: 스마트폰(최신 Android Chrome) 웹앱. 링크로 열고, 홈 화면에 추가해서 앱처럼 쓴다.
- 대회 제출용이라 "링크를 열면 바로, 오류 없이 동작하는 것"이 가장 중요하다.

## 2. 용어 (프로젝트 전체에서 통일)
집 > 층(도면) > 방 > 가구 > 보관함 > 물건
- 집: 여러 개 가능. 층: 집마다 여러 개, 층마다 도면 사진 1장. 층이 1개면 층 선택 단계는 건너뛴다.
- 방: 도면 위에 꼭짓점을 하나씩 탭해서 그리는 다각형. 이름이 있다.
- 가구: 방 안에 배치하는 수납가구. 사진(촬영 또는 갤러리)과 이름. 방 화면에서 위치를 옮길 수 있다.
- 보관함: 가구 사진 위에 그리는 사각형 영역. 이름, 이동, 크기 조절 가능.
- 물건: 이름 + (사진 또는 이모티콘). 메모·수량 등 다른 정보는 두지 않는다.

## 3. 기술 스택
- React + TypeScript + Vite, Tailwind CSS, zustand. 도면·가구 사진 위의 도형은 SVG 오버레이로 그린다.
- 백엔드는 Firebase 무료 요금제(Spark)의 Realtime Database + 익명 로그인 + Hosting이고, 연결은 P5에서 한다. Cloud Storage, Cloud Functions, 유료 요금제, 자체 서버, 계정 로그인 화면은 쓰지 않는다.
- 사진은 기기에서 압축한 뒤 base64 문자열(data URL)로 Realtime Database에 저장한다.
- 데이터 접근은 하나의 repository 인터페이스 뒤에 숨긴다. 지금은 메모리(로컬) 구현만 만들고, P5에서 Firebase 구현을 추가한다. 화면 코드는 어떤 구현인지 몰라야 한다. (나중에 "체험하기" 모드가 같은 인터페이스의 로컬 구현을 쓴다.)
- 모바일 우선(폭 360~430px), 세로 화면. 앱 안의 모든 문구는 한국어.

## 4. 데이터 모델
모든 항목에 id(uuid), createdAt, updatedAt을 둔다. 좌표는 이미지 기준 0~1 비율로 저장한다.
- House: name, ownerUid
- Floor: houseId, name, order, planPhotoId
- Room: floorId, name, points[{x,y}]
- Furniture: roomId, name, photoId, x, y (방 화면 캔버스 위치)
- Bin: furnitureId, name, x, y, w, h
- Item: binId, name, emoji 또는 photoId
- Photo: id, dataUrl, updatedAt (다른 항목과 분리된 별도 컬렉션. 필요할 때만 불러온다.)
- 부모를 삭제하면 하위 항목과 사진도 함께 삭제한다. (확인 팝업 필수)

## 5. 공통 UX 규칙
- 모든 화면 상단바: 뒤로가기(←), 제목, 홈(🏠). 홈을 누르면 어디서든 메인 화면으로 간다.
- 뒤로가기는 화면 단계를 되돌아간다. 폰의 뒤로가기 버튼도 같은 동작(History API). 메인에서 한 번 더 뒤로가면 "앱을 종료하시겠습니까?" 팝업(아니오/예)을 띄운다. 웹은 앱을 닫을 수 없으니 "예"는 브라우저의 뒤로가기를 허용하는 것으로 한다.
- 도면·가구 사진 화면에는 보기/편집 모드 토글이 있다.
  - 보기 모드: 탭 = 들어가기, 두 손가락 = 확대/축소(핀치 줌), 한 손가락 드래그 = 화면 이동
  - 편집 모드: 탭 = 선택, 길게 누르기(약 0.8초) 후 드래그 = 이동, 모서리 핸들 = 크기 조절, 빈 곳 드래그 = 화면 이동
- 색: 방 파랑, 가구 초록, 보관함 주황 (반투명 채움 + 테두리). 깜빡임은 밝은 노랑, 0.8초 주기.
- 빈 화면에는 다음 행동을 안내하는 문구와 버튼을 보여준다.
- 삭제는 확인 팝업. 사진 권한 거부, 인터넷 끊김, 저장 실패는 한국어 안내 메시지로 처리한다. (앱이 멈추면 안 된다.)
- 터치 대상은 최소 44px. 밝고 깔끔한 디자인, 글자는 크게.

## 6. 작업 규칙
- 프롬프트 한 번에 한 기능 묶음만 만든다. 끝나면 "폰에서 직접 확인하는 방법"을 단계별로 알려준다.
- 요청하지 않은 기능은 추가하지 않는다. 모호하면 질문한다.
- 끝날 때마다 git 커밋한다.
- 샘플·아이콘 이미지는 코드로 직접 그린 SVG만 쓴다. (외부 이미지, 저작권 있는 자료 금지)
- 비밀 값은 .env(git 제외)와 .env.example로 관리한다. Firebase 웹 설정값은 공개돼도 되는 값이고, 보호는 보안 규칙이 한다.

## 7. 이번 범위에서 만들지 않는 것
계정 로그인, 오프라인 사용, iOS 대응, 다국어, 실행 취소, 방 꼭짓점 수정(삭제 후 다시 그림), 모든 집에서 검색, 물건 이동, 초성 검색, 유료 서비스.

## 8. 진행 상황
- P1 (완료): Vite + React + TS + Tailwind + zustand 프로젝트 골격, 데이터 모델 타입, repository 인터페이스 + 메모리 구현, 화면 이동 스택(History API 연동 + 종료 확인 팝업), 공통 컴포넌트(상단바/확인 팝업/빈 상태/토스트/로딩), 메인 화면(집 목록/빈 상태).
- P2 (완료): 메인 화면(집 카드 목록/추가/이름 수정/삭제), 집 화면(층 목록/추가/이름 수정/삭제, 마지막 층 삭제 방지), 층 화면(도면 촬영·가져오기·교체, 보기/편집 모드, 핀치 줌·드래그 이동, 방 다각형 그리기·이름 변경·삭제), compressImage 유틸(재사용 가능), 방 화면은 자리표시만.
  - 새 집을 만들면 "1층"이 자동 생성되고 그 층 화면으로 바로 이동한다(집에 층이 0개인 상태는 없음).
  - 층이 1개인 집을 메인에서 탭하면 집 화면을 건너뛰고 바로 층 화면으로 간다. 층 화면 상단바의 "층 관리"(🗂) 버튼으로는 층이 1개여도 항상 집 화면(층 목록)을 열 수 있다.
  - PromptDialog(이름 입력용 공용 컴포넌트), PhotoCanvas(핀치 줌/팬/탭 제스처를 처리하는 공용 사진 캔버스, 가구·보관함 화면에서도 재사용 예정)를 추가했다.
- 이후 단계(P3~): 방 화면(가구 배치/사진), 가구·보관함 화면, 물건 등록, 검색·깜빡임 안내, Firebase 연결(P5), 가족 공유 코드.
