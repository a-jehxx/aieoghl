@AGENTS.md

# 프로젝트 개요

> 0단계 산출물. 기술 스택, 데이터 구조, 화면, 기능 상세는 다음 단계에서 순서대로 정리한다.
> 코딩을 시작하기 전, 아래 내용 중 모호하거나 서로 충돌하는 부분이 있으면 반드시 질문할 것.

## 1. 앱 정의
집 안의 물건이 어느 방, 어느 가구, 어느 보관함에 있는지 도면과 사진으로 기록하고,
물건을 검색하면 그 위치를 단계별 깜빡임으로 안내해 주는 가족용 수납 위치 관리 앱.

## 2. 사용자와 핵심 사용 장면
- 사용자: 집을 정리하며 물건 위치를 기록하는 사람, 그리고 집에서 물건 위치를 찾는 사람. (같은 사람일 수도 있음)
- 장면 1: 검색창에 "볼펜심"을 입력하면 그 물건이 들어 있는 위치가 단계별로 깜빡이며 안내된다.
- 장면 2: 집을 정리한 뒤, 물건을 어느 보관함에 뒀는지 바로 기록한다.

## 3. 용어와 계층 구조 (프로젝트 전체에서 통일해서 사용)
집 > 층(도면) > 방 > 가구 > 보관함 > 물건
- 집: 여러 개 추가 가능. 메인 화면에서 집을 선택한다.
- 층: 집마다 여러 개 추가 가능. 층마다 도면(사진) 한 장을 가진다. 층이 하나뿐이면 층 선택 단계는 자동으로 건너뛴다.
- 방: 도면 위에 꼭짓점을 하나씩 클릭해 다각형으로 범위를 지정한다. 방마다 이름을 붙인다.
- 가구: 방 안에 있는 수납가구. 가구 사진을 촬영하거나 갤러리에서 가져온다.
- 보관함: 가구 사진 위에 영역을 그려서 만든다. (크기 조절, 위치 이동 가능)
- 물건: 이름 + 사진 또는 이모티콘. 물건에 저장하는 정보는 이름과 이미지뿐이고, 메모·수량·별칭·유통기한은 두지 않는다.

## 4. 진입 방식 (로그인 없음)
계정 로그인 없이 기기(휴대폰)로 사용자를 구분한다. 앱을 처음 열면 두 가지 중 하나를 선택한다.
1. 새 집 만들기: 도면을 직접 업로드해서 시작한다.
2. 가족 코드로 참여: 가족이 만든 집의 코드를 입력해서 그 집에 들어간다.

## 5. 공유와 권한
- 공유 단위는 집이다. 집마다 공유 코드가 있고, 코드로 들어온 사람에게 그 집의 모든 데이터가 공유된다.
- 공유된 구성원은 모두 같은 권한을 가진다. (조회, 편집, 검색 모두 가능. 조회 전용 역할은 없다.)
- 공유를 그만하는 기능("가족공유 그만하기")이 있다.

## 6. 검색과 안내
- 검색 대상은 물건 이름뿐이다.
- 기본은 지금 선택한 집 안에서 검색하고, 옵션으로 "모든 집에서 검색"을 둔다.
- 안내는 선이 아니라 깜빡임으로 한다. 검색한 물건이 든 방이 깜빡이고, 방에 들어가면 가구가, 가구에 들어가면 보관함이, 보관함에 들어가면 그 물건이 깜빡인다.

## 7. 시각 원칙
- 방은 경계선을 그린 뒤 안쪽을 살짝 색으로 채워서 영역을 알아볼 수 있게 한다. (깜빡임도 이 영역이 깜빡이는 것)
- 방, 가구, 보관함은 서로 다른 색 계열로 구분한다. 구체적인 색은 5단계(공통 UX 규칙)에서 정한다.

## 8. 플랫폼과 동작 환경
- 핸드폰에 설치하는 네이티브 앱. 개발 환경은 Windows, 테스트 기기는 Android 폰이다. (Mac이 없어서 Android를 대상으로 한다.)
- 데이터는 서버에 하나만 두고, 모든 폰이 실시간으로 읽고 쓴다.
- 인터넷이 없으면 사용할 수 없다. 연결이 없을 때는 안내 화면을 보여준다.
- 직접 서버를 만들지 않고, 관리형 백엔드 서비스를 사용한다.

## 9. 아직 정하지 않은 것 (진행하면서 결정)
- 초기 등록 수고의 허용 수준, 이번에 하지 않을 것, 성공 기준
- 기술 스택과 백엔드 서비스의 구체적인 선택 (1단계에서 결정)
